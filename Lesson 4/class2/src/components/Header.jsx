// This is how we can import the React.Component class from the React library
// So this works exactly the same as the previous example
import { Component } from "react";

class Header extends Component {
  render() {
    return <h1>Header</h1>;
  }
}

export default Header;
